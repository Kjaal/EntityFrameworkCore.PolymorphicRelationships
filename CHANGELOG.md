# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Harden runtime consistency for temporary/store-generated polymorphic keys
- Enforce `morphOne` and polymorphic pivot uniqueness at runtime and in tests
- Expand provider execution coverage across PostgreSQL, SQL Server, and MySQL
- Keep native `Select(...)` polymorphic projection support experimental and explicit opt-in
